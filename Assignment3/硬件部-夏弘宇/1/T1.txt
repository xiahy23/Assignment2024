比较MVC设计模式与本讲与之前一讲提到的前端模式，前、后端分别与MVC中的哪个（或哪些）部分相似？

答：前端一般是图形化界面，起到与用户交互的功能，与View相似
后端一般用来处理业务逻辑，并且还会有数据库，从这个意义上，和Model相似；当然Controller的作用是作为Model和View的桥梁，这部分功能在前后端的模式中也由后端提供API来实现，因此后端也兼具Controller的功能。

